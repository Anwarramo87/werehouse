{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "backend-nest/**",
      "Dockerfile",
      "docker-entrypoint.sh",
      "railway.json"
    ]
  },
  "deploy": {
    "preDeployCommand": "node scripts/prisma-safe.js remote migrate deploy",
    "restartPolicyType": "ON_FAILURE"
  }
}
